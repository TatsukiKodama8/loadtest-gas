```bash
# リポジトリを作成する
git init

# typescript/claspをインストールする
npm i -D typescript @types/google-apps-script
npm i -g @google/clasp

# claspログイン
clasp login

# クローンする
clasp clone <SCRIPT_ID>
```
スクリプトIDはサイドバーにある歯車マークの「設定」画面にある

次の構成にする
- `src/`: TS。編集対象。
- `dist/`: JS。生成物でこれをpushする。

```bash
## Apps ScriptにpushするJS
mkdir -p src dist

## tsconfig.jsonを作成する
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "None",
    "lib": ["ES2020"],
    "types": ["google-apps-script"],
    "strict": true,
    "rootDir": "src",
    "outDir": "dist",
    "removeComments": false,
    "noImplicitAny": true
  },
  "include": ["src/**/*.ts"]
}

## clspのrootDirをdistにする
## 例：いろいろ書いてあるけど下のように.clasp.jsonを編集する
{
  "scriptId": "<SCRIPT_ID>",
  "rootDir": "dist"
}

```

